# RM Clipboard for Linux

The Linux installer creates and uses:

```text
/home/<username>/Documents/Support Messages/RM/
```

Run `./install.sh` to install the GTK/Python launcher, import missing templates,
and bind it to **Super+Shift+M** in i3.
